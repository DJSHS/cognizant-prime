server:
  port: 8002

spring:
  application:
    name: speed-service
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
    hibernate:
      ddl-auto: create
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/travelblog.cjhzkqqc8ctk.us-east-1.rds.amazonaws.com
    username: postgres
    driver-class-name: org.postgresql.Driver
    password: revature
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      


      
      

      
